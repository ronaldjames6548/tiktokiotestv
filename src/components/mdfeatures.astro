---
import Link from "@components/ui/link.astro";
// Remove old astro-i18next imports
// import { localizePath } from "astro-i18next";
// import { t } from "i18next";

// Add new built-in i18n imports
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import { getRelativeLocaleUrl } from "astro:i18n";

// @ts-ignore
import { Icon } from "astro-icon/components";

// Get current language and create translation function
const safeUrl = Astro.url || new URL('/', Astro.site || 'http://localhost:4321');
const lang = getLangFromUrl(safeUrl);
const t = useTranslations(lang);

// Helper function to create localized paths (if needed)
function localizePath(path: string) {
	if (!path || (path && path.startsWith('http')) || path === '#') return path;
	return getRelativeLocaleUrl(lang, path);
}
const createMarkup = (text: string) => ({ __html: text });
const features = [
	
];
---

<div class="relative p-4">
      <div class="max-w-screen-xl mx-auto">
            <div class="feature-box backdrop-blur-sm">
				<div itemprop="itemListElement" itemscope itemtype="https://schema.org/HowToStep" class="feature-box backdrop-blur-sm">
					<div class="text-center py-8">
						<h2 class="text-2xl md:text-3xl font-bold text-gray-900"><span class="text-pink-700">{t('md.h2')}</span> {t('md.h2_1')}</h2>
                        <meta itemprop="url" content="/" />
                        <meta itemprop="timeRequired" content="PT1M"/>
						<div class="w-24 h-1 mx-auto bg-pink-700 rounded-full"></div>
						<p class="text-left mt-4 mx-auto text-lg text-gray-900 dark:text-white" set:html={t('md.text1')} />
					</div>
	    <section id="about" class="mb-16" style="opacity: 1;">
			<div class="grid md:grid-cols-2 gap-8">
				<div clas="transform: none;">
					<div class="rounded-lg border-color-blue shadow bg-card text-card-foreground shadow-sm h-full">
					<div itemtype="https://schema.org/HowToStep" class="how-to-card rounded">
						<div class="flex flex-col space-y-1.5 p-6">
							<div class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
								<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-info h-5 w-5 text-blue-500">
								<circle cx="12" cy="12" r="10"></circle><path d="M12 16v-4">
								</path><path d="M12 8h.01"></path>
								</svg>
								{t("how.works_h2")}
								
							</div>
						</div>
						<div class="p-6 pt-0">
							<p class="mb-4" set:html={t('how.works')} />
							<ol class="list-decimal list-inside space-y-2 text-muted-foreground">
								<li class="mb-4" set:html={t('how.step1')} />
								<li class="mb-4" set:html={t('how.step2')} />
								<li class="mb-4" set:html={t('how.step3')} />
								<li class="mb-4" set:html={t('how.step4')} />
								<li class="mb-4" set:html={t('how.step5')} />
							</ol>
						</div>
					</div>
					</div>
				</div>
				<div style="transform: none;">
					<div class="rounded-lg border-color-blue shadow bg-card text-card-foreground shadow-sm h-full">
						<div class="flex flex-col space-y-1.5 p-6">
							<div class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
								<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-info h-5 w-5 text-blue-500">
								<circle cx="12" cy="12" r="10"></circle>
								<path d="M12 16v-4"></path>
								<path d="M12 8h.01"></path>
								</svg>
								{t("feature.h2")}
							</div>
						</div>
						<div class="p-6 pt-0">
							<ul class="space-y-3">
								<li class="flex items-start gap-2">
									<div class="w-12 h-12 rounded-lg flex items-center justify-center"> <img src="/images/unlimited.svg" alt="Unlimited Downloads" class="w-10 h-10 mb-4"> </div>
									<span>{t('infinity.download')}</span>
								</li>
								<li class="flex items-start gap-2">
									<div class="w-12 h-12 rounded-lg flex items-center justify-center"> <img src="/images/check-mark.svg" alt="No Watermark" class="w-10 h-10 mb-4"> </div>
									<span>{t('no.watermark')}</span>
								</li>
								<li class="flex items-start gap-2">
									<div class="w-12 h-12 rounded-lg flex items-center justify-center"><img src="/images/fast.svg" alt="Fast Tiktok Downloader" class="w-10 h-10 mb-4"> </div>
									<span>{t('faste.download')}</span>
								</li>
								<li class="flex items-start gap-2">
									<div class="w-12 h-12 rounded-lg flex items-center justify-center"><img src="/images/audio.svg" alt="Fast Tiktok Downloader" class="w-10 h-10 mb-4"> </div>
									<span>{t('audio.download')}</span>
								</li>
								<li class="flex items-start gap-2">
									<div class="w-12 h-12 rounded-lg flex items-center justify-center"><img src="/images/all.svg" alt="Fast Tiktok Downloader" class="w-10 h-10 mb-4"> </div>
									<span>{t('content.types')}</span>
								</li>
							</ul>
						</div>
					</div>
				</div>
			</div>
		</section>
    </div>
	</div>
</div>
</div>
<!--Sention 1 End -->

